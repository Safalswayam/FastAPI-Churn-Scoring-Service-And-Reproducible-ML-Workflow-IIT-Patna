# Churn UI

This is a React + Vite frontend for the churn scoring API.

## Quick start

```bash
cd part4_api/ui
npm install
npm run dev
```

The app calls the FastAPI server through the Vite dev proxy. Make sure the API
is running at http://localhost:8000.
